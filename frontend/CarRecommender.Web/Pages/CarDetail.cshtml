@* 
    DYNAMISCHE DETAILPAGINA - ROUTE MET PARAMETER:
    ===============================================
    
    ROUTE: @page "/car/{id}" 
    - De {id} is een route parameter die dynamisch wordt ingevuld vanuit de auto-kaart
    - Voorbeeld: /car/123 -> id = 123
    
    ID DOORGEVEN VAN KAART NAAR DETAILPAGINA:
    ==========================================
    1. In kaart componenten (Index.cshtml, AdvancedFilters.cshtml, Cars.cshtml):
       - var carId = recommendation.Car.Id (of car.Id)
       - Links gebruiken: href="/car/@carId" of onclick="window.location.href='/car/@(carId)'"
       - Elke kaart heeft zijn eigen unieke carId uit recommendation.Car.Id
    
    2. Route matching:
       - URL /car/123 wordt gematcht met @page "/car/{id}"
       - Razor Pages mapt automatisch {id} naar de int id parameter in OnGetAsync(int id)
    
    3. In CarDetailModel.OnGetAsync(int id):
       - De id parameter bevat de waarde uit de URL (bijv. 123)
       - Deze id wordt gebruikt om GET /api/cars/{id} aan te roepen
       - De API retourneert de juiste auto data op basis van deze id
    
    VOOR: Hardcoded naar één BMW X5 (alle kaarten linkten naar dezelfde auto)
    NU: Elke kaart linkt naar zijn eigen detailpagina via unieke car.Id
*@
@page "/car/{id:int}"
@model CarDetailModel
@{
    ViewData["Title"] = Model.Car != null ? $"{Model.Car.Brand} {Model.Car.Model}" : "Auto Details";
    var apiBaseUrl = Model.ApiBaseUrl;
}

@functions {
    string FormatTransmission(string? transmission)
    {
        if (string.IsNullOrWhiteSpace(transmission))
            return "Niet opgegeven";
        
        string lower = transmission.ToLower().Trim();
        
        if (lower.Contains("automatic") || lower.Contains("automaat") || lower.Contains("automatisch") || lower.Contains("cvt") || lower.Contains("dct"))
            return "Automaat";
        
        if (lower.Contains("manual") || lower.Contains("handmatig") || lower.Contains("schakel") || lower.Contains("handbak"))
            return "Handmatig";
        
        return char.ToUpper(transmission[0]) + (transmission.Length > 1 ? transmission.Substring(1).ToLower() : "");
    }
}

<div class="container mt-4">
    @if (!string.IsNullOrEmpty(Model.ErrorMessage))
    {
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            <strong>Fout:</strong> @Model.ErrorMessage
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    @if (Model.Car != null)
    {
        <div class="row mb-3">
            <div class="col-12">
                <a href="/" class="btn btn-secondary">
                    <i class="bi bi-arrow-left"></i> Terug naar zoeken
                </a>
            </div>
        </div>

        <div class="row">
            <div class="col-12">
                <h1 class="display-4 mb-4">@Model.Car.Brand @Model.Car.Model</h1>
            </div>
        </div>

        <div class="row">
            <div class="col-md-6">
                @if (Model.Images.Count > 0)
                {
                    <div id="carImageCarousel" class="carousel slide mb-4" data-bs-ride="carousel">
                        <div class="carousel-inner">
                            @for (int i = 0; i < Model.Images.Count; i++)
                            {
                                var imageSrc = Model.Images[i];
                                // Als image relatief pad is (begint met /), voeg API base URL toe voor Azure
                                if (!string.IsNullOrEmpty(imageSrc) && imageSrc.StartsWith("/") && !string.IsNullOrEmpty(apiBaseUrl))
                                {
                                    imageSrc = apiBaseUrl + imageSrc;
                                }
                                <div class="carousel-item @(i == 0 ? "active" : "")">
                                    <img src="@imageSrc" 
                                         class="d-block w-100" 
                                         alt="@Model.Car.Brand @Model.Car.Model - Foto @(i + 1)"
                                         style="height: 400px; object-fit: cover; background-color: #1a1a1a;"
                                         onerror="this.src='https://via.placeholder.com/800x400/0066CC/FFFFFF.png?text=@Uri.EscapeDataString(Model.Car.Brand + " " + Model.Car.Model)'">
                                </div>
                            }
                        </div>
                        @if (Model.Images.Count > 1)
                        {
                            <button class="carousel-control-prev" type="button" data-bs-target="#carImageCarousel" data-bs-slide="prev">
                                <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                                <span class="visually-hidden">Vorige</span>
                            </button>
                            <button class="carousel-control-next" type="button" data-bs-target="#carImageCarousel" data-bs-slide="next">
                                <span class="carousel-control-next-icon" aria-hidden="true"></span>
                                <span class="visually-hidden">Volgende</span>
                            </button>
                        }
                    </div>

                    <div class="mb-4">
                        <p class="text-muted">
                            <strong>@Model.Images.Count</strong> foto@(Model.Images.Count != 1 ? "'s" : "") beschikbaar
                        </p>
                    </div>
                }
                else
                {
                    <div class="mb-4">
                        <img src="https://via.placeholder.com/800x400/0066CC/FFFFFF.png?text=@Uri.EscapeDataString(Model.Car.Brand + " " + Model.Car.Model)" 
                             class="img-fluid" 
                             alt="@Model.Car.Brand @Model.Car.Model">
                    </div>
                }
            </div>

            <div class="col-md-6">
                <div class="card">
                    <div class="card-body">
                        <h3 class="card-title mb-4">Auto Details</h3>
                        <dl class="row">
                            <dt class="col-sm-5">Prijs:</dt>
                            <dd class="col-sm-7">€@Model.Car.Budget.ToString("N0")</dd>

                            <dt class="col-sm-5">Brandstof:</dt>
                            <dd class="col-sm-7">@Model.Car.Fuel</dd>

                            <dt class="col-sm-5">Bouwjaar:</dt>
                            <dd class="col-sm-7">@Model.Car.Year</dd>

                            <dt class="col-sm-5">Vermogen:</dt>
                            <dd class="col-sm-7">@Model.Car.Power PK</dd>

                            <dt class="col-sm-5">Transmissie:</dt>
                            <dd class="col-sm-7">@FormatTransmission(Model.Car.Transmission)</dd>

                            @if (!string.IsNullOrWhiteSpace(Model.Car.BodyType))
                            {
                                <dt class="col-sm-5">Carrosserie:</dt>
                                <dd class="col-sm-7">@Model.Car.BodyType</dd>
                            }

                            @if (!string.IsNullOrWhiteSpace(Model.Car.DriveType))
                            {
                                <dt class="col-sm-5">Aandrijving:</dt>
                                <dd class="col-sm-7">@Model.Car.DriveType</dd>
                            }
                        </dl>
                    </div>
                </div>
                
                @* Ratings Sectie *@
                <div class="card mt-4">
                    <div class="card-body">
                        <h4 class="card-title mb-3">Beoordelingen</h4>
                        @if (Model.Rating != null && Model.Rating.TotalRatings > 0)
                        {
                            <div class="mb-3">
                                <div class="d-flex align-items-center mb-2">
                                    <div class="me-3">
                                        <span class="display-6 fw-bold">@Model.Rating.AverageRating.ToString("F1")</span>
                                        <span class="text-muted">/ 5.0</span>
                                    </div>
                                    <div>
                                        @for (int i = 1; i <= 5; i++)
                                        {
                                            <i class="bi @(i <= Math.Round(Model.Rating.AverageRating) ? "bi-star-fill text-warning" : "bi-star text-muted")"></i>
                                        }
                                    </div>
                                </div>
                                <p class="text-muted mb-0">Gebaseerd op @Model.Rating.TotalRatings beoordeling@(Model.Rating.TotalRatings != 1 ? "en" : "")</p>
                            </div>
                            
                            <div class="mb-3">
                                @for (int stars = 5; stars >= 1; stars--)
                                {
                                    var count = stars == 5 ? Model.Rating.FiveStarRatings :
                                                stars == 4 ? Model.Rating.FourStarRatings :
                                                stars == 3 ? Model.Rating.ThreeStarRatings :
                                                stars == 2 ? Model.Rating.TwoStarRatings :
                                                Model.Rating.OneStarRatings;
                                    var percentage = Model.Rating.TotalRatings > 0 ? (count * 100.0 / Model.Rating.TotalRatings) : 0;
                                    
                                    <div class="d-flex align-items-center mb-1">
                                        <span class="me-2" style="width: 60px;">@stars <i class="bi bi-star-fill text-warning"></i></span>
                                        <div class="progress flex-grow-1 me-2" style="height: 20px;">
                                            <div class="progress-bar bg-warning" role="progressbar" style="width: @percentage.ToString("F0")%"></div>
                                        </div>
                                        <span class="text-muted" style="width: 50px; text-align: right;">@count</span>
                                    </div>
                                }
                            </div>
                        }
                        else
                        {
                            <p class="text-muted">Nog geen beoordelingen voor deze auto.</p>
                        }
                        
                        @* Rating Formulier *@
                        <div class="mt-4">
                            <h5>Geef je beoordeling</h5>
                            <div class="mb-3">
                                <div class="rating-stars" data-car-id="@Model.Car.Id">
                                    @for (int i = 1; i <= 5; i++)
                                    {
                                        <i class="bi bi-star rating-star" data-rating="@i" style="font-size: 2rem; cursor: pointer; color: #ccc; margin-right: 5px;"></i>
                                    }
                                </div>
                                <p class="text-muted mt-2" id="rating-text">Klik op een ster om te beoordelen</p>
                            </div>
                            <button type="button" class="btn btn-primary" id="submit-rating" disabled>Beoordeling verzenden</button>
                            <div id="rating-message" class="mt-2"></div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        @if (Model.Images.Count > 1)
        {
            <div class="row mt-4">
                <div class="col-12">
                    <h3>Alle Foto's</h3>
                    <div class="row">
                        @foreach (var imageUrl in Model.Images)
                        {
                            var imageSrc = imageUrl;
                            // Als image relatief pad is (begint met /), voeg API base URL toe voor Azure
                            if (!string.IsNullOrEmpty(imageSrc) && imageSrc.StartsWith("/") && !string.IsNullOrEmpty(apiBaseUrl))
                            {
                                imageSrc = apiBaseUrl + imageSrc;
                            }
                            <div class="col-md-3 col-sm-4 col-6 mb-3">
                                <img src="@imageSrc" 
                                     class="img-thumbnail" 
                                     alt="@Model.Car.Brand @Model.Car.Model"
                                     style="height: 200px; object-fit: cover; cursor: pointer;"
                                     onclick="showImageModal('@imageSrc')"
                                     onerror="this.src='https://via.placeholder.com/300x200/0066CC/FFFFFF.png?text=@Uri.EscapeDataString(Model.Car.Brand + " " + Model.Car.Model)'">
                            </div>
                        }
                    </div>
                </div>
            </div>
        }
    }
</div>

<!-- Image Modal -->
<div class="modal fade" id="imageModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-lg modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">@(Model.Car?.Brand) @(Model.Car?.Model)</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body text-center">
                <img id="modalImage" src="" class="img-fluid" alt="@(Model.Car?.Brand) @(Model.Car?.Model)">
            </div>
        </div>
    </div>
</div>

<script>
    function showImageModal(imageUrl) {
        document.getElementById('modalImage').src = imageUrl;
        var modal = new bootstrap.Modal(document.getElementById('imageModal'));
        modal.show();
    }

    // Rating functionaliteit
    document.addEventListener('DOMContentLoaded', function() {
        const carId = @(Model.Car?.Id ?? 0);
        if (carId === 0) return; // Geen auto, skip rating functionaliteit
        
        let selectedRating = 0;
        const stars = document.querySelectorAll('.rating-star');
        const submitButton = document.getElementById('submit-rating');
        const ratingText = document.getElementById('rating-text');
        const ratingMessage = document.getElementById('rating-message');
        const apiBaseUrl = '@Model.ApiBaseUrl';
        const ratingStarsContainer = document.querySelector('.rating-stars');

        if (!stars.length || !submitButton || !ratingText || !ratingStarsContainer) {
            console.error('Rating elements not found');
            return;
        }

        function highlightStars(count) {
            stars.forEach((star, index) => {
                if (index < count) {
                    star.classList.remove('bi-star');
                    star.classList.add('bi-star-fill');
                    star.style.color = '#ffc107';
                } else {
                    star.classList.remove('bi-star-fill');
                    star.classList.add('bi-star');
                    star.style.color = '#ccc';
                }
            });
        }

        // Star hover en click events
        stars.forEach((star, index) => {
            star.addEventListener('mouseenter', function() {
                highlightStars(index + 1);
            });

            star.addEventListener('click', function() {
                selectedRating = index + 1;
                highlightStars(selectedRating);
                submitButton.disabled = false;
                ratingText.textContent = `Je hebt ${selectedRating} ster${selectedRating !== 1 ? 'ren' : ''} geselecteerd`;
            });
        });

        // Reset stars op mouseleave (behoud geselecteerde rating)
        ratingStarsContainer.addEventListener('mouseleave', function() {
            highlightStars(selectedRating);
        });

        // Submit rating
        submitButton.addEventListener('click', async function() {
            if (selectedRating === 0) return;

            submitButton.disabled = true;
            submitButton.textContent = 'Verzenden...';
            ratingMessage.innerHTML = '';

            try {
                const response = await fetch(`${apiBaseUrl}/api/ratings`, {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify({
                        carId: carId,
                        rating: selectedRating
                    })
                });

                if (response.ok) {
                    ratingMessage.innerHTML = '<div class="alert alert-success">Bedankt voor je beoordeling! De pagina wordt vernieuwd...</div>';
                    setTimeout(() => {
                        window.location.reload();
                    }, 1500);
                } else {
                    const error = await response.json();
                    ratingMessage.innerHTML = `<div class="alert alert-danger">Fout: ${error.error || 'Onbekende fout'}</div>`;
                    submitButton.disabled = false;
                    submitButton.textContent = 'Beoordeling verzenden';
                }
            } catch (error) {
                ratingMessage.innerHTML = `<div class="alert alert-danger">Fout bij het verzenden: ${error.message}</div>`;
                submitButton.disabled = false;
                submitButton.textContent = 'Beoordeling verzenden';
            }
        });
    });
</script>

